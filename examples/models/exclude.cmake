# Per-category exclusions for examples-upstream/models
#
# All 29 upstream [models] examples build and run on rayvulkan, so this list is
# empty. Keep the variable defined so the parent CMakeLists include() stays valid.
set(RAYVK_EXAMPLES_EXCLUDE_EXTRA
)
