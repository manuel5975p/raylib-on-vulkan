# others: per-category exclusions (see CMakeLists.txt rayvk_add_example_category).
# None. raylib_opengl_interop and rlgl_standalone are excluded globally in
# CMakeLists.txt, not here. embedded_files_loading builds and passes. See STATUS.md.
set(RAYVK_EXAMPLES_EXCLUDE_EXTRA)
